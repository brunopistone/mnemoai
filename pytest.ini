[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    integration: tests that require a live Ollama server and a real config.yaml (deselect with -m "not integration")
filterwarnings =
    ignore::DeprecationWarning
